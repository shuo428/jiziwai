<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="springbootjni.mapper.UserMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="springbootjni.entity.User">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="created_at" jdbcType="TIMESTAMP" property="createdAt" />
        <result column="updated_at" jdbcType="TIMESTAMP" property="updatedAt" />
        <result column="deleted" jdbcType="BOOLEAN" property="deleted" />
    </resultMap>
    
    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, username, password, created_at, updated_at, deleted
    </sql>
    
    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_user
        WHERE username = #{username}
        AND deleted = false
    </select>
    
    <!-- 根据ID查询用户 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_user
        WHERE id = #{id}
        AND deleted = false
    </select>
    
    <!-- 插入用户 -->
    <insert id="insert" parameterType="springbootjni.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, created_at, updated_at, deleted)
        VALUES (#{username}, #{password}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, false)
    </insert>
    
    <!-- 更新用户 -->
    <update id="update" parameterType="springbootjni.entity.User">
        UPDATE t_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
        AND deleted = false
    </update>
    
    <!-- 逻辑删除用户 -->
    <update id="deleteById">
        UPDATE t_user
        SET deleted = true,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
    
</mapper>
